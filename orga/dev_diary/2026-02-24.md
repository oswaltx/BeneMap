## **Backend (Spring Boot + Kotlin)**

- **Moved backend to `backend/` folder**
    - Fixed IntelliJ module and source/resource roots
    - Corrected package structure so `DemoApplication.kt` is recognize
- **Configured Gradle**
    - Updated `settings.gradle` to include backend module
    - Added dependencies for REST, JPA, H2, Kotlin reflection, and tests
- **Created REST API**
    - Added `MainController` with `@RestController` and `@CrossOrigin`
    - Exposed endpoints like `/api` and `/`
    - Tested that endpoints are reachable from Svelte dev server
- **Database setup**
    - Created `VolunteerActivity` entity with `@Entity` and `@Id`
    - Used H2 in-memory database for development
    - Configured datasource in `application.properties`
        
- **Debugged common issues**
    - Fixed `DemoApplication` not found after folder/module move
    - Verified Gradle builds and module dependencies
    - Made sure sources/resources roots are correct
        

---

## **Frontend (Svelte)**

- **Set up Svelte in `frontend/`**
    - Confirmed SPA structure with `App.svelte` as main entry
    - Decided not to use backend templates; all UI is handled in Svelte
    - Discussed modularity and reusable components
        
- **Connected to backend**
    - Configured Svelte to fetch data from `/api` endpoint
    - Enabled CORS in backend for `localhost:5173`
        
- **Routing & Pages**
    - Talked about options for multiple pages (router or conditional rendering)
    - Confirmed single-page structure for now
        

---

## **Integration & Collaboration**

- **Frontend ↔ Backend communication**
    - REST API returns JSON for Svelte to consume
    - Backend handles data persistence and business logic
    - Frontend handles all HTML rendering
        
- **Collaboration ready**
    - Modular Svelte components → easier for multiple devs
    - Clear backend API → multiple clients can consume